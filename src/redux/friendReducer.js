let initialState = {
    online: [
        {name: 'Victor', id: 2},
        {name: 'Roman', id: 3},
        {name: 'Andrey', id: 4}
    ]
};

const friendReducer = (state = initialState, action) => {


    return state
}

export default friendReducer;